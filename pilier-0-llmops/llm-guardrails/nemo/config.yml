models:
  - type: main
    engine: openai
    model: llama3.1:8b
    parameters:
      openai_api_base: "http://localhost:11434/v1"
      openai_api_key: "ollama"

rails:
  input:
    flows:
      - check jailbreak attempt
      - check dangerous request
  output:
    flows:
      - check dangerous output
